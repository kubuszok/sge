<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal manifest for the ISS-562 androidSign scripted test. aapt2 link only
  needs a valid manifest with a package; the launcher activity mirrors a real
  hello-world game layout (aapt2 does not verify the class exists).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example">

    <application android:label="Hello SGE Android">
        <activity
            android:name="com.example.HelloGame"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
